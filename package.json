{
  "name": "virtual-marketer-static",
  "version": "1.0.0",
  "description": "Virtual Marketer - High-performance static HTML website",
  "main": "scripts/build.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "clean": "rm -rf dist-tmp",
    "build": "node scripts/build.js && node scripts/generate-blog-posts.js && node scripts/seo-optimize.js && node scripts/generate-sitemap.js",
    "analyze": "node scripts/analyze.js",
    "optimize": "node scripts/optimize.js"
  },
  "keywords": ["static", "site", "performance", "optimization"],
  "author": "Virtual Marketer",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
